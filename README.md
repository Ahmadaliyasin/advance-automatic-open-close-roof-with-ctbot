Important: this CTbot can only use **arduinoJson 6.19**. If you use the latest ArduinoJson versions, they cannot be used and will cause errors when compiling the Arduino.
Before using the code, first create the bot on Telegram with @Botfather and obtain the API token. Then, copy the API token into the code.
