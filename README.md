Important: this CTbot can only use **arduinoJson 6.19**. If you get the latest ArduinoJson versions, it cannot be used and will error when you compile the Arduino
Before using the code, first create the bot on Telegram with @Botfather and obtain the API token. Then, copy the API token into the code.
